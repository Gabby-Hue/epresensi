import { useEffect } from 'react';import { ActivityIndicator,View } from 'react-native';import { router } from 'expo-router';import { supabase } from '@/lib/supabase';
export default function Index(){useEffect(()=>{(async()=>{const {data:{session}}=await supabase.auth.getSession();if(!session)return router.replace('/(auth)/login');const {data}=await supabase.from('profiles').select('role').eq('auth_user_id',session.user.id).single();router.replace(data?.role==='admin'?'/(admin)':'/(user)')})()},[]);return <View style={{flex:1,justifyContent:'center'}}><ActivityIndicator/></View>}
